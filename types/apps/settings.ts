export interface SettingsProps {
  isDarkMode?: boolean;
}

export interface SettingsThemeClasses {
  textColor: string;
  bgColor: string;
  sidebarBg: string;
  cardBg: string;
  hoverBg: string;
  secondaryText: string;
  subtleText: string;
}
